# Local gateway that exposes Ollama + web_search tools over OpenAI /v1
GATEWAY_BASE_URL=http://127.0.0.1:8000

# Any non-empty string is accepted by the gateway
GATEWAY_API_KEY=ollama

# Default model shown in the playground settings
DEFAULT_MODEL=qwen3.6:latest
